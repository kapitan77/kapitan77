class order:
  def __init__(self,*cart,customer):
    self.cart=list(cart)
    self.customer = customer
  def __len__(self):
    return self.cart.__len__() 
  def __bool__(self):
   return bool(self .cart)
